# SKillXC 仓库规则

## 仓库定位

- 本仓库是 AI 影视生产 Skill 的中央源仓库，不是具体影视项目，也不是项目数据目录。
- `skills/` 是 30 个生产 Skill 的唯一内容源；安装位置只允许链接到这里，不得维护副本。
- `.codex-plugin/plugin.json` 是整套 Skill 的插件清单。
- 具体项目的数据、资产登记表、脚本事实索引和生成记录不得写入本仓库。

## 调用规则

- 生产任务先进入 `production-router-handoff`，由它判断当前阶段和最小调用集合。
- 混合或类别不清的世界资产需求由 `world-asset-production` 二级路由拆分；它不生产候选。道具/载具、生物/怪物、VFX 分别由 `prop-vehicle-production`、`creature-monster-production`、`vfx-asset-production` 唯一负责。
- 除总路由入口外，专业 Skill 和通用 Skill 必须在向用户说明用途并获得确认后显式调用。
- 每次只加载当前阶段真正需要的 Skill；不得一次加载全部 Skill 或全部大师 Profile。
- 默认使用轻量生产模式：后台完整维护任务单、稳定ID/版本、索引和校验，普通对话只展示当前创作结果、必要缺口、一个确认点与下一步。只有用户明确要求项目绑定、登记、控制版本、进度/失败记录、正式审计、正式QC或复盘时，才生成最小申请/同步包并转入相应严格记录对话；简化展示不得跳过任何硬规则。
- 大师 Profile 只在阶段合同允许且用户本次明确选择后调取，并且只在指定阶段和作用域内生效。导演在项目风格意图与场戏调度分别询问；摄影 Profile 只在建立/重建全片摄影基线时常规询问，后续继承摄影规则卡，除非用户明确确认限时场戏例外。所有 Profile 到本体失效点后关闭，下游只接收已转译的项目规则切片。
- `shot-visual-design` 是分镜表唯一写入者；`cinematography-direction` 先建立全片/场戏摄影规则，后做逐镜摄影审核，不直接改分镜表或重新决定叙事功能。
- `acting-direction` 是角色表演母档和场戏表演卡的唯一写入者；重要角色在剧作/导演场戏工作前按项目建立一份当前有效母档，逐场只做适配，不把母档当作人物或声音资产。
- `sound-voice-direction` 是角色声音身份卡、独立声音候选卡和场戏声音卡的唯一写入者。只有台词、独白或持续画外发声角色才按需建立独立声音资产；身份卡不是资产，环境/动作声与Seedance临时镜头内声音默认不登记。发声角色缺少正式声音资产时必须询问用户选择补做候选或临时声音，不得自行决定。
- 中央源定义、Profile卡版本和项目控制卡版本必须分离。专业Skill写控制内容，`creative-control-versioning` 唯一写项目长期创作基线、表演母档与声音身份索引的版本和状态；四张风格长期基线固定为“风格无上游、美术只引风格、摄影只引美术、灯光只引摄影”，顾问任务成果独立引用，P0评审和风格组合快照进入任务成果索引；中央源/Profile升级不得自动改写既有项目卡。
- `art-lookdev-direction` 把美术决定分为永久世界规则、资产类别规则、场次美术状态和镜头临时效果；美术只负责物质世界、固有色与可见社会逻辑，不决定摄影机、光源或镜头临时效果。资产类别执行包只是部门交接，不能代替P0评审或最终风格包放行。
- `lighting-direction` 是项目灯光基线、场景光源地图、人物受光卡、多视图灯光继承卡和镜头灯光短执行卡的唯一写入者。场景空间只提供窗、灯具、反射面等空间锚点；摄影定义相机侧曝光保护与动态范围目标，灯光以来源、填充、反射和负补光实现；VFX定义自发光本体与时序，灯光只负责环境承接。光源地图是任务数据集，不是图片资产。
- `specialist-consultant-router` 先把专业点分为必须准确、允许艺术化或纯虚构规则，每个问题只路由一个主专业领域；问题包、证据集和结论卡独立版本化并进入任务成果索引。没有可靠来源时保持“暂不可裁决”，下游只精确引用当前可交接结论卡的主张／采用字段。现实危险内容只保留非操作性现象、后果、安全禁区与专业复核需求，不扩写成可执行伤害指南。
- `dramaturgy-scene-beats` 是场戏逐节拍数据与场戏节拍卡的唯一写入者；先形成 `BDT@版本`，再形成精确引用它的 `DSB@版本`。它保持剧本内容版本、原文定位、锁定台词、事件顺序与因果，只向 `directing-blocking` 和 `acting-direction` 模式 B 交付戏剧依据，不决定景别、构图、机位、焦段、运镜、灯光、剪辑或正式 Prompt。
- `directing-blocking` 是 `DBD@版本` 场戏调度数据与 `DBC@版本` 场戏调度卡的唯一写入者；按节拍建立观众知情关系、人物起止位置、距离、朝向、视线、遮挡、出入场、移动触发、权力空间变化、轴线和银幕方向。导演 Profile 在本阶段重新询问并于 DBC 交付时关闭；它不决定具体焦段，不建立表演母档，不写分镜表，缺少空间依据时提出 SV3 缺口返回资产计划/场景生产。
- `editing-rhythm` 是 `ERT@版本` 剪辑关系数据与 `ERC@版本` 剪辑节奏卡的唯一写入者；逐镜说明顺序、时长、可定位切点、动作/声音接点、相邻状态与生成单元替代，不按平均时长机械切分或自动把对白做成正反打。剪辑只提出修订，`shot-visual-design` 唯一写回分镜，写回后才由 `cinematography-direction` 做逐镜摄影审核。
- `video-prompt-production` 先为当前生成单元建立 `VRM@版本` 媒体参考顺序/唯一用途映射，再建立 `VPR@版本` 单镜头或镜头组正式Prompt；每名人物只调用一张匹配当前服装/状态的五视图总览卡。Prompt只翻译当前精确上游，不重新导演；Seedance能力在使用时核对，当前节点只交用户手工复制，不提交、查询或下载任务。
- `video-qc-review` 只做即时诊断、项目复盘或用户明确授权的正式QC记录；视频、截图和明确文字反馈必须分别声明核验边界。每条问题只选择一个主根因层与唯一责任节点，QC不直接改上游；即时诊断/复盘分析默认不落盘，失败原因只有用户明确要求才保存，镜头可用结论不要求上传视频且不得由AI推断。
- 分镜组、镜头和正式 Prompt 使用稳定 ID 与独立版本；Prompt“已放行生成”和生成结果“可用”不得合并为一个“通过”。生产对话只维护轻量检查点，明确同步后由 `generation-version-log` 专用记录对话写项目进度表。
- 没有具体项目或项目数据目录时，只维护工作流，不创建虚构项目数据。
- 具体项目必须先由 `approval-asset-registry` 确认项目 ID、项目数据根目录和绑定卡；所有项目数据读写必须解析到绑定目录内，禁止跨项目串表。
- 项目数据默认遵循 `skills/production-router-handoff/references/project-data-contract.md`：人读卡、CSV 行数据、JSON Schema 检查三层分工；结构检查不得替代批准和业务复核。`tasks/任务索引.csv` 只负责任务调度；无专用权威表的任务产物按 `skills/production-router-handoff/references/task-artifact-index.md` 写入 `tasks/任务成果索引.csv`，任务与成果是一对多关系。

## 维护边界

- `docs/剩余工作与完成验收总表_v0.1.md` 是真人写实第一版后续建设的唯一范围、顺序和完成判据；它只用于仓库维护，不参与具体项目创作推理。新增、删除、合并、拆分或重排工作包必须先取得用户确认，不能用无总数的“进入下一项”扩展范围。
- 用户要求按优先级一次修复一项时，只修改当前确认项；完成逐文件检查后停止，等待下一次确认。
- 修改 Skill 前遵循 Codex 的 `skill-creator` 规范；修改插件结构前遵循 `plugin-creator` 规范。
- 修改源文件后必须检查 UTF-8、YAML/JSON 语法、内部链接、目录结构和调用边界。
- 修改数据模板或 Schema 后必须更新 `skills/production-router-handoff/assets/data-contract-map.json`，并运行 `validate_project_data.py --manifest`；不得只增加 Schema 而不执行检查。
- `ARCHIVE_EXPLICIT_ONLY`：历史讨论、聊天转录和接续记录不属于本仓库、插件或任何 Skill 的引用图。未收到用户在当前指令中明确提供的归档路径和“读取／核对／比较历史”要求时，禁止从外部目录、Git 历史、旧提交、远端旧版本或缓存中搜索、读取或引用这些内容。
- 新任务的现行入口及其加载策略以 `active-rule-sources.json` 为准；列入清单不代表一次加载全部内容，Skill 正文仍须遵守“元数据发现后、用户确认再激活”的抽屉式边界。当前规则以用户最新明确指令、正式 Skill 和现行流程文件为准。
- 提交前运行 `python deployment/validate_active_rule_boundary.py`；不得把历史正文、历史文件链接或通向外部归档的目录链接重新放入现行仓库。

## 安装拓扑

- 用户级 `.agents` 的持久化根目录位于 `D:\Ai影视制作\2026项目\006-skills`；Windows 用户目录使用指向它的根入口链接，或在应用占用根目录时使用分别指向 D 盘的 `skills`、`plugins` 子入口链接。
- 30 个 SKillXC Skill 使用持久化根目录中的目录链接，目标必须指向本仓库 `skills/` 下的对应 Skill。
- 个人插件市场中的 `skillxc` 入口指向本仓库根目录；不得复制整个仓库形成第二份内容源。
- 重装恢复工具的仓库源位于 `deployment/windows/`，D 盘运行副本位于持久化根目录的 `recovery/`。
- 直接链接安装和插件安装不得同时启用，以免同名 Skill 重复发现。
- 开发态固定采用直接 Skill 链接：个人市场可保留 `skillxc` 的 `AVAILABLE` 条目，但 `skillxc@personal` 必须保持未安装且不得存在安装缓存。恢复验收必须同时检查 30 项运行时目录和新对话初始提示；不得用磁盘存在替代运行时发现证据。
- 更新 Skill 时编辑中央源仓库，再运行验证；不要在安装链接位置建立独立版本。
