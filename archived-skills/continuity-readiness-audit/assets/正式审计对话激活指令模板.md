使用 `$continuity-readiness-audit`。这是严格记录模式的正式就绪审计对话，只对下列范围执行完整连续性/就绪检查并把依据写入绑定项目；不创作资产、表演、分镜或 Prompt，不解除资产待复核状态。

项目ID：【填写】
项目数据绑定卡：【绝对路径与版本】
审计范围：【场次 / 生成单元；精确ID@版本】
用户保存正式审计原文：【填写】
审计输入切片：【绝对路径或粘贴完整内容】

重新核对绑定、当前正式资产、有效控制卡、任务成果和版本传播状态。只写本范围的审计卡、场次/生成单元就绪表与校验报告；没有明确依据的结论标为缺失，不能从轻量结论反推详细记录。
