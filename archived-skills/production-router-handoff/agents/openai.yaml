interface:
  display_name: "生产路由与交接"
  short_description: "轻量路由当前任务，后台保留完整交接，并按授权进入严格记录。"
  default_prompt: "使用 $production-router-handoff 以轻量生产模式路由当前任务，只显示必要决定点并在后台保留完整交接。"
policy:
  allow_implicit_invocation: true

