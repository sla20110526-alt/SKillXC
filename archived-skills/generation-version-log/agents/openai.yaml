interface:
  display_name: "镜头进度与生成记录"
  short_description: "在独立记录对话中按授权同步镜头进度，或保存详细生成版本与失败原因。"
  default_prompt: "使用 $generation-version-log 核对同步包，按我的明确授权更新镜头进度或详细生成记录。"
policy:
  allow_implicit_invocation: false

