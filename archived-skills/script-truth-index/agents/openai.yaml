interface:
  display_name: "剧本事实索引"
  short_description: "建立并校验场次、原台词、状态、连续性与出处事实索引。"
  default_prompt: "使用 $script-truth-index 为当前锁定剧本建立可追溯事实索引，并校验场次与事实表。"
policy:
  allow_implicit_invocation: false

