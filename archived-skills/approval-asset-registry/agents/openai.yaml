interface:
  display_name: "资产审批与登记"
  short_description: "按明确授权登记资产，并传播父版本变化和派生资产待复核状态。"
  default_prompt: "使用 $approval-asset-registry 核对项目绑定、登记或兼容确认原文，原子更新正式资产、可调用映射和状态传播表。"
policy:
  allow_implicit_invocation: false

