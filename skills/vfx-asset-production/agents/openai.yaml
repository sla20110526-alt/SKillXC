interface:
  display_name: "VFX视觉资产生产"
  short_description: "用GPT Image 2建立可复用VFX的视觉身份与时空行为基准。"
  default_prompt: "使用 $vfx-asset-production 生产当前VFX视觉基准资产候选。"
policy:
  allow_implicit_invocation: false
