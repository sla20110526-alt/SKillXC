# VFX资产生产卡

项目ID：
项目数据绑定卡路径与版本：
任务ID与任务单版本：
需求槽位ID：
独立资产判定：建立VFX图片资产 / 单镜执行交接 / 待确认
VFX工作模式：独立资产判定 / 视觉母版 / 状态/强度变体 / 接触参考 / 时序与接触定义 / 单镜执行交接 / 版本兼容复核 / 登记回执绑定
本轮预留正式资产ID：不适用 / 【填写】
资产角色：不适用 / VFX视觉母版 / VFX状态变体 / VFX接触参考
交付形态：不适用 / 单张VFX视觉参考卡 / 行为分阶段设计板（仅审查） / 单镜执行条款
当前候选状态：待输入 / 待设计确认 / 待Prompt / 待外部生成 / 待用户判断 / 继续调整 / 已确认任务数据 / 待明确登记指令 / 已提交登记申请

## 已确认事实与输入

VFX名称、剧情功能与出现范围：
触发者、来源、介质与因果：
现实/艺术化/超自然边界：
初始尺度、影响范围和方向：
当前有效VFX短执行卡ID与版本：
顾问结论ID与版本：不适用 / 【填写】
仍会改变身份、时序、接触或安全的缺口：无 / 【填写】

## 定义与版本依据

VFX定义ID与版本：待建立 / 【填写】
父VFX正式ID@版本：不适用 / 【填写】
来源/触发实体正式ID@版本：不适用 / 【填写】
接触实体/空间/光影正式ID@版本：不适用 / 【填写】
时序集ID与版本、表路径和校验结论：待建立 / 【填写】
接触集ID与版本、表路径和校验结论：待建立 / 【填写】
登记回执正式资产ID与版本：未登记 / 【填写】
本候选实际持有的任务数据绑定状态：不适用（沿用父数据集） / 未绑定 / 已绑定【正式资产ID@版本】

## 视觉与物理身份

材料类别、主体轮廓和内部层级：
颗粒/碎屑尺度、密度、透明度和边缘：
颜色来源、自发光/受光与相对亮度：
传播、重力、浮力、阻力、湍流和衰减：
跨镜必须保持：
允许随阶段变化：
禁止无来源变化：

## 本轮唯一变化或执行范围

本镜可见阶段、时长与阶段转折：不适用 / 【填写】
来源位置、方向、速度和尺度变化：不适用 / 【填写】
接触区域、作用、即时反应和次级运动：不适用 / 【填写】
发光、环境受光、曝光保护和恢复：不适用 / 【填写】
残留、稳定结束状态与相邻镜头承接：不适用 / 【填写】
PRESERVE EXACTLY：
CHANGE：
ONLY CHANGE：

## Prompt与候选检查

参考图用途映射：
- 参考1：文件/正式版本；用途；禁止继承
- 参考2：文件/正式版本；用途；禁止继承
唯一候选定位：未生成 / 【文件路径或对话内唯一定位】
检查结论：来源 / 轮廓 / 尺度 / 材料层级 / 密度 / 透明度 / 边缘 / 颜色 / 发光 / 接触 / 环境反馈 / 背景可分离性
可见问题与用户调整方向：无 / 【填写】
失败原因是否获用户授权记录：否 / 是，转generation-version-log
下一步：继续调整 / 确认任务数据 / 等待明确登记指令 / 输出登记申请 / 交镜头与剪辑 / 兼容复核 / 返回上游

说明：行为分阶段设计板、时序表、接触表和单镜执行条款不是正式图片资产。只有用户对唯一图片候选明确说“登记这张”“确认登记”等才能提交登记申请。收到登记回执后必须以“登记回执绑定”模式把正式 `ID@版本` 写回本卡，以及 `预留正式资产ID` 等于本候选 ID 的时序/接触数据行；状态图沿用父资产数据集时不得改写父数据行。本候选实际持有的数据未绑定时不得交给下游。

## 数据契约

对应CSV：`VFX时序阶段表模板.csv`、`VFX环境接触表模板.csv`
对应Schema：`vfx-temporal.schema.json`、`vfx-contact.schema.json`
数据契约版本：v0.1
校验报告：`validation/【任务ID】-vfx-validation.md`
