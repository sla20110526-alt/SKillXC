使用 `$generation-version-log`。这是严格记录模式的详细生成记录专用对话，只保存用户明确指定范围内的生成批次、Prompt/参考/设置、结果定位、失败原因或版本变化；不修改资产、分镜或 Prompt，不推断用户未说明的失败原因。

项目ID：【填写】
项目数据绑定卡：【绝对路径与版本】
记录范围：【图片 / 视频 / 独立声音；稳定Prompt ID@版本；候选或镜头定位】
用户记录授权原文：【填写】
详细记录输入：【同步包/记录包路径，或粘贴完整内容】

重新读取绑定卡，只写明确授权范围。把用户判断与 AI 诊断分开；缺少文件、版本或唯一对象时暂停。结构检查通过后返回记录卡、正式记录路径、校验报告和未记录缺口。
