# 镜头字段说明

- 叙事任务与存在理由先于摄影参数。
- 场景视图必须命中正式可调用视图；缺失时返回补做 SV3。
- 景别必须带对象，如“角色A近景”“双人中景”“场景远景”。
- 距离、视场与透视描述可见结果，毫米焦段仅作辅助。
- 运镜包含物理方式、唯一主要意图、触发点和停止点。
- 第一帧与结束状态必须稳定、可见、可供相邻镜头承接。
- 调用资产记录正式资产 ID、版本和平台引用名。

