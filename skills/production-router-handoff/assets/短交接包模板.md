# 短交接包

项目ID：
任务ID@任务单版本：
当前阶段：
唯一责任Skill：
完整任务单路径：

当前唯一目标：
最小数据切片（名称｜精确ID@版本｜项目内路径）：
用户尚需决定：无 / 【填写一个决定点】
禁止事项：
返回条件：

## 短激活指令

使用 `$【唯一责任Skill】`，只处理 `【任务ID@任务单版本】` 的当前唯一目标。先读取 `【完整任务单路径】`，随后只读取本短包列出的最小数据切片；不要加载其他 SKillXC Skill 或任何未列明 Profile。默认使用轻量生产模式，只向我显示本轮创作结果、必要缺口和一个确认点，不展开任务单、Schema、索引或后台检查日志。若需要扩大范围，停止并返回 `$production-router-handoff`。完成后按本短包的返回条件交回。
