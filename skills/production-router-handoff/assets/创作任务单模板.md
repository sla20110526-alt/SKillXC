# 创作任务单

项目与项目锁定卡版本：
项目ID：
任务ID与任务单版本：
项目数据绑定卡路径与版本：
当前阶段：
当前目标：
完成定义：
需求槽位/场次/镜头范围：
分镜组ID与版本：不适用 / 【填写】
镜头ID与版本集合：不适用 / 【填写】
生成单元ID：不适用 / 【填写】
正式PromptID与版本：不适用 / 未建立 / 【填写】
已确认输入及版本：
必须继承：
允许变化：
禁止变化：
准备调用的通用 Skill：
用户是否已确认使用：
用户选择的 Profile 与作用范围：
摄影工作模式：不适用 / 全片摄影基线 / 场戏摄影约束 / 逐镜摄影审核
全片摄影规则卡引用：不适用 / 未建立 / 【填写】
场戏摄影约束卡引用：不适用 / 未建立 / 【填写】
摄影 Profile 状态：不适用 / 待在全片摄影基线询问 / 仅通用 Skill / 继承规则卡不重载 / 用户已确认限时场戏例外
输出格式：
格式例外：无 / 【用户指定格式及原因】
下游接收 Skill：
返回条件：
状态记录方式：生产对话轻量检查点 / 导出同步包 / 用户已明确授权独立记录对话同步
缺口与阻塞项：
任务索引路径：
对应Schema：`handoff-task.schema.json`
数据契约版本：v0.1
校验报告路径：
校验结论：通过 / 未通过

## 新对话激活指令

只处理本任务单范围，只读取列出的最小数据切片。不得切换项目数据目录、重拆整个剧本、改变项目类别、改写预留正式资产ID、把候选或失败结果登记为正式资产，也不得在用户未确认时加载大师Profile。摄影工作模式不是“全片摄影基线”时，不重复询问或读取完整摄影Profile；已确认的限时场戏例外除外。Prompt放行和生成结果可用必须分开表达；生产对话不得写镜头生产进度表，除非本任务明确是 `generation-version-log` 专用记录任务且携带用户同步授权原文。

