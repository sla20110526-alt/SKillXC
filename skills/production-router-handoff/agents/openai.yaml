interface:
  display_name: "生产路由与交接"
  short_description: "核对项目绑定，路由最小Skill，并校验跨对话数据交接包。"
  default_prompt: "使用 $production-router-handoff 核对项目绑定，路由当前任务，生成任务索引并校验最小交接包。"
policy:
  allow_implicit_invocation: true

