使用 `$approval-asset-registry`。这是严格记录模式的项目绑定专用对话，只建立或核对用户明确指定的项目数据根目录、绑定卡和必要空索引；不进行资产创作、登记或项目内容补写。

项目名称与项目ID：【填写】
用户指定项目数据根目录：【绝对路径】
用户绑定确认原文：【填写；没有明确确认时只生成待确认卡】

重新核对目标目录不在 SKillXC 仓库、Skill 安装目录或其他项目数据目录内。已有绑定和数据只检查，不用空模板覆盖；未明确绑定时不得写成已绑定。完成后只返回绑定卡路径/版本、状态、已初始化的必要索引和任何阻断项。
