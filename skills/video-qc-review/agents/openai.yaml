interface:
  display_name: "视频QC审查"
  short_description: "按需诊断Seedance镜头；明确记录时校验QC表，默认不保存。"
  default_prompt: "使用 $video-qc-review 检查当前生成镜头；不要自动记录，若我明确要求记录则校验QC表。"
policy:
  allow_implicit_invocation: false

